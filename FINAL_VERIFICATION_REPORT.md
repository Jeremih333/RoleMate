# Финальный отчёт проверки RoleMate

Дата: 29 июля 2026 года.

## Выпуск 0026–0031: социальная лента, уведомления, рейтинги и контекстная модерация

Кандидат `b70b89bb39cc03f7bcbb9ec85f1b996d820621dc` полностью проверен и
опубликован в production.

- Посты получили медиакарусель, ответы на комментарии, лайки/дизлайки
  комментариев, проверенные отметки владельца и модераторов, теги, фандомы и
  хештеги.
- Лента постов ранжирует активный контент по совпадению тегов и фандомов с
  активными анкетами пользователя, рейтингу поста и числу комментариев.
  Ограниченный охват понижает пост, теневой бан исключает его из чужой ленты.
- Ограничение охвата, теневой бан, восстановление и блокировка доступны
  модератору как прямо в ленте, так и в очереди постов админ-панели. Все действия
  проходят серверную проверку прав и записываются в audit log.
- Лайк и суперсимпатия записываются как положительная оценка конкретной анкеты,
  крестик — как дизлайк. Существующие реакции безопасно перенесены на основные
  анкеты; собственные оценки и оценки чужих анкет изолированы.
- Жалобы привязываются к точной анкете, посту, комментарию, ответу или чату.
  Очередь модератора раскрывает полный пост и ветку комментариев, а для
  анонимного чата показывает только безопасные метаданные сообщений — полный
  текст анонимной переписки не логируется.
- Добавлен единый центр уведомлений с колокольчиком, отметкой прочтения,
  уведомлениями об упоминаниях, комментариях и сообщениях. Реальные
  RoleMate-юзернеймы подсвечиваются и ведут на единый профиль независимо от
  числа назначенных псевдонимов.
- Premium-альбом Telegram сохраняется как один пост с медиакаруселью. Попытка
  бесплатного пользователя отправить альбом отклоняется явным сообщением и не
  создаёт несколько постов.
- Публичный профиль показывает активные анкеты и посты, поддерживает собственный
  рейтинг и прямое начало чата. В профиле доступны «Мои анкеты» и управление
  несколькими анкетами по Premium-лимиту.
- Плеер музыки в профиле упрощён: оставлена компактная кнопка
  воспроизведения/паузы, проблемные нативные тайминги и регулятор громкости
  удалены.

Миграции `0026`–`0031` сначала применены к `rolemate-preview`, затем к
`rolemate-production`. До production-применения сохранён D1 Time Travel
bookmark:
`0000000e-000014b7-000050b7-dce4ab7484cfa40a699bcfaecbf2cf54`.

После обеих стадий:

- `PRAGMA foreign_key_check` не вернул нарушений;
- потерянных связей медиакарусели и уведомлений нет;
- недопустимых значений режима охвата нет;
- все шесть миграций присутствуют в `d1_migrations`.

Production:

- Data API version: `2362fdb0-60ed-4fbc-9f62-a89efd2f6146`;
- App Worker version: `25c3e509-c9c3-45ee-9906-fd46e1114b59`;
- `/health/ready`: `ready`, D1 dependency `true`;
- новый asset bundle: `index-B0sQTHR0.js`;
- закрытые маршруты постов, уведомлений и жалоб без сессии возвращают `401`.

Проверка кандидата:

| Проверка                         | Результат                              |
| -------------------------------- | -------------------------------------- |
| Secret scan                      | passed                                 |
| Prettier                         | passed                                 |
| ESLint                           | passed                                 |
| TypeScript `strict`              | passed                                 |
| Unit/integration/migration tests | 85 passed                              |
| Playwright E2E                   | 144 passed: Android, iPhone и desktop  |
| Production build                 | passed                                 |
| Dependency audit                 | известных уязвимостей нет              |
| Preview D1 `0026`–`0031`         | applied, foreign keys and links valid  |
| Production D1 `0026`–`0031`      | applied, foreign keys and links valid  |
| Production smoke                 | readiness, assets and auth gates valid |

## Выпуск 0024–0025: лента, юзернеймы, профиль и редактор постов

Кандидат полностью проверен и опубликован в production. Обычная загрузка App
Worker дважды получила HTML-ответ Cloudflare API `403`; минифицированный bundle
успешно прошёл тот же API и был активирован.

- Исправлена адаптивная шапка профиля: внутренний ID больше не сжимается по одной
  букве в строке. «Редактировать профиль» открывает форму, плавно прокручивает к
  ней и переводит фокус в поле имени.
- Поиск снова открывается на вкладке «Анкеты» и показывает вертикальную ленту
  полноценных карточек с медиашапкой, музыкой и релевантным порядком. В поиске
  остались только «Анкеты» и «Профили»; отдельная лента постов сохранена.
- Лайк больше не удаляет анкету из выдачи, а входящие симпатии остаются видимыми
  после ответного лайка. Полноэкранная медиакарусель получила стрелки и
  клавиатурную навигацию.
- Добавлены уникальные RoleMate-юзернеймы: обычному пользователю доступен один
  адрес от 5 символов, владелец может назначить до пяти адресов, включая
  четырёхсимвольные. Все адреса ведут на один публичный профиль и участвуют в
  поиске. Владелец отмечен золотой галочкой, модератор — синей.
- Исправлена кнопка создания поста в обычном меню бота. Владелец поста может
  открыть «Настройки поста», изменить заголовок и Markdown-текст, заменить медиа
  через защищённый deep link бота или удалить медиа. Чужое редактирование
  блокируется на сервере.
- Миграции `0024` и `0025` сначала применены на preview, затем на production.
  Production Time Travel bookmark перед применением:
  `0000000e-00000525-000050b7-e59015cdf0f827d8ce0b86b697a3228e`.
  `PRAGMA foreign_key_check` не выявил нарушений; потерянных переносов текста
  постов нет.
- Data API production version:
  `812cc5b5-d1a0-4599-8e9f-22ebeb48090b`.
- App Worker production version:
  `ed3663ce-d69b-4df3-b303-f67b7e477a05`.

Проверка кандидата:

| Проверка                         | Результат                             |
| -------------------------------- | ------------------------------------- |
| Secret scan                      | passed                                |
| Prettier                         | passed                                |
| ESLint                           | passed                                |
| TypeScript `strict`              | passed                                |
| Unit/integration/migration tests | 79 passed                             |
| Playwright E2E                   | 129 passed: Android, iPhone и desktop |
| Production build                 | passed                                |
| Dependency audit                 | известных уязвимостей нет             |
| Preview D1 `0024`, `0025`        | applied, foreign keys valid           |
| Production D1 `0024`, `0025`     | applied, data and foreign keys valid  |
| Data API production              | deployed                              |
| App Worker production            | deployed with minified Worker bundle  |

## Выпуск 0023: сессии, единый поиск и модерация публичного контента

Выпуск проверен и опубликован в production.

- Исправлена причина `INVALID_CSRF`: действующая HttpOnly-сессия теперь безопасно
  продлевается с ротацией CSRF-токена при запуске MiniApp и каждые восемь минут.
  Сохранение профиля и загрузка симпатий используют уже обновлённую сессию.
- Поиск получил четыре доступные вкладки: «Всё» по умолчанию, «Профили»,
  «Анкеты» и «Посты». Поиск поддерживает текст и точный внутренний ID, не создаёт
  горизонтального переполнения на узком экране и сохраняет отдельный карточный
  режим анкет.
- Вкладка «Профиль» стала публичным предпросмотром: внутренний ID, аватар,
  описание, количество анкет и постов, опубликованные посты, кнопки
  «Редактировать профиль» и «Создать пост».
- Аватар выбирается из принадлежащих пользователю одобренных фото или коротких
  видео. Новое медиа и пост создаются через защищённый сценарий бота.
- В модерирование добавлены отдельные очереди публичных профилей, анкет и постов
  с поиском по внутреннему ID, Telegram ID, username или имени. Блокировки и
  восстановления записываются в audit log; модератор по-прежнему не может видеть
  или изменять других модераторов и владельца.
- Миграция `0023` только добавляет статус и причину модерации публичного профиля;
  пользовательские данные не удаляются и не переписываются.
- Production Time Travel bookmark перед `0023`:
  `0000000d-00000894-000050b7-2c61243fa6a27f8f245e01ca4a743732`.
- После миграции сохранены все 4 публичных профиля, все имеют допустимый статус,
  сохранена 1 анкета, `PRAGMA foreign_key_check` не вернул нарушений.

## Выпуск 0022: отдельные профили, анкеты и посты

Код полностью проверен и опубликован в production. Причиной первоначальной
ошибки входа оказался старый ограниченный `CLOUDFLARE_API_TOKEN`, который
перекрывал уже сохранённую рабочую OAuth-сессию Wrangler. OAuth-сессия имеет
права `workers_scripts:write` и `d1:write`. App Worker был опубликован
минифицированным bundle: неминифицированный multipart-запрос ошибочно
блокировался Cloudflare API с HTML-ответом `403`, тогда как тот же bundle с
`--minify` успешно принят и развёрнут.

- Добавлен отдельный публичный `user_profiles` с внутренним UUID, описанием и
  аватаром, не раскрывающий Telegram ID.
- Анкеты вынесены в `questionnaires`: бесплатно доступна одна, с Premium — до
  пяти. После истечения Premium дополнительные анкеты принудительно выключаются,
  но данные не удаляются.
- Поиск читает все активные анкеты и ранжирует их отдельно; начало прямого чата
  проверяет именно видимую новую анкету, а не устаревшую legacy-запись.
- Добавлены лента постов, фото/видео/GIF/аудио, метаданные трека, комментарии и
  независимые рейтинги постов и анкет.
- Медиа анкеты открывается полноэкранно с сохранением пропорций; видео имеет
  нативные controls.
- `/menu` содержит отдельные подписанные маршруты «Мой профиль», «Мои анкеты» и
  «Посты».
- Preview D1 успешно применил `0022`; `PRAGMA foreign_key_check` не вернул
  нарушений. Legacy-таблицы не изменены и остаются источником отката.
- Production Time Travel bookmark перед `0022`:
  `0000000d-0000045c-000050b7-334b57834162a0d857f7f6d1cd126191`.
- Production D1 успешно применил `0022`: перенесена 1 из 1 legacy-анкеты,
  потерянных анкет и медиа нет, дубликатов primary-анкет нет,
  `PRAGMA foreign_key_check` не вернул нарушений.

Проверка кандидата:

| Проверка                         | Результат                             |
| -------------------------------- | ------------------------------------- |
| Secret scan                      | passed                                |
| Prettier                         | passed                                |
| ESLint                           | passed                                |
| TypeScript `strict`              | passed                                |
| Unit/integration/migration tests | 76 passed                             |
| Playwright E2E                   | 123 passed: Android, iPhone и desktop |
| Production build                 | passed                                |
| Dependency audit                 | известных уязвимостей нет             |
| Preview D1 `0023`                | applied, foreign keys valid           |
| Production D1 `0023`             | applied, data and foreign keys valid  |

## Итог

Текущая версия RoleMate собрана, проверена и опубликована в Cloudflare. Production
работает как единое Cloudflare-приложение с отдельным закрытым Worker API для D1;
Northflank и банковская карта не являются обязательными зависимостями.

- MiniApp и Bot API: <https://rolemate-app.carreljeremih.workers.dev>
- App Worker version: `78692e86-8aa6-425d-bfee-518fe170fb2b`
- D1 Data API: <https://rolemate-data-api.carreljeremih.workers.dev>
- Data API version: `c62ae6a1-9ba0-45f2-893f-d0d1775b7edb`
- Preview Data API version: `d2a41068-4766-497e-b2c4-c84518f3bff5`
- D1 production и preview: миграции `0001`–`0023` применены, ожидающих миграций нет.
- Production Time Travel bookmark перед `0023`:
  `0000000d-00000894-000050b7-2c61243fa6a27f8f245e01ca4a743732`.
- Production Time Travel bookmark перед `0022`:
  `0000000d-0000045c-000050b7-334b57834162a0d857f7f6d1cd126191`.
- Production Time Travel bookmark перед `0019`–`0021`:
  `0000000c-000003fb-000050b7-9cf2cee41ffe0b88d26ba35a5156adef`.
- Preview Time Travel bookmark перед `0019`–`0021`:
  `0000000a-00000002-000050b7-40f414334d0743d73f91302433f3a323`.

## Что вошло в выпуск

- Исправлены поиск и ранжирование анкет: анкеты других пользователей видимы,
  возрастная изоляция удалена, сверху показываются более релевантные результаты.
- Поставленные ранее `like` и `super_like` больше не исключают анкету из поиска;
  явный пропуск, взаимные блокировки, отключение и модерационные ограничения
  продолжают скрывать её.
- Добавлены поиск по ключевым словам, тегам и псевдониму, пользовательские теги,
  ввод через запятую для языков, жанров, фандомов и целей поиска.
- Анкеты публикуются автоматически; медиа больше не ждёт обязательной
  предварительной модерации. Владелец и модератор могут предупредить, заблокировать
  на 24 часа или отключить анкету прямо из поиска.
- Медиа анкеты показывается общей листаемой шапкой; аудио имеет проигрыватель.
  После истечения Premium сервер немедленно применяет бесплатные ограничения.
- Бесплатная анкета поддерживает два фото или видео. Premium открывает до восьми
  медиафайлов, музыку с названием, исполнителем и обложкой, а первый GIF/видеофайл
  автоматически проигрывается в выдаче только при действующем Premium.
- Пользователь сам меняет порядок файлов карусели. Шапка собственной анкеты,
  предпросмотр и публичная карточка используют один и тот же порядок без дублей.
- Полную анкету можно открыть из выдачи и сразу начать анонимный чат без ожидания
  взаимного лайка; такой чат не маскируется под взаимную симпатию.
- Входящие симпатии доступны без Premium в MiniApp и через `/matches`, привязаны к
  внутреннему UUID профиля и не зависят от изменяемого Telegram username.
- Суперсимпатии доступны всем: бесплатный лимит — 1 в сутки, Premium-лимит
  настраивается владельцем и по умолчанию равен 5. MiniApp и бот показывают
  остаток/исчерпание лимита, а получатель видит отдельное уведомление.
- Добавлены профильные фото- и видеоаватары. Видео проверяется по лимитам 6 секунд,
  8 МБ и 720×720 и отображается как бесшумная зацикленная GIF-анимация.
- Исправлен запуск разделов из `/menu`: бот создаёт компактную HMAC-подписанную,
  ограниченную по времени и привязанную к Telegram ID и маршруту ссылку прямо в
  URL-пути, поэтому Telegram-клиенты не могут потерять её как query-параметр. MiniApp
  сначала использует действующую HttpOnly-сессию, затем безопасный menu launch,
  а после этого Telegram `initData`. Подмена маршрута, подписи и срока действия
  отклоняется.
- Добавлены предпросмотр собственной анкеты и понятное описание сюжетных вариантов.
- После успешного действия «Сохранить и опубликовать» кнопка становится зелёной с
  галочкой и текстом «Опубликовано!». При новом редактировании состояние сбрасывается.
- Исправлены переполнение мобильной вёрстки и выпадающий список часовых поясов;
  добавлены понятные подписи городов и аватары вместо буквенных заглушек.
- Premium показывает активный статус, дату окончания и точное количество оставшихся
  дней. Бесплатный буст доступен один раз в сутки и сообщает о повторной попытке.
- Покупка, выдача и подарок Premium отправляют получателю сообщение с точным числом
  начисленных дней. Цифровой Premium внутри Telegram оплачивается только Stars.
- Промокод резервируется до фактической успешной оплаты, не сгорает при брошенном
  счёте, применяется только к выбранным тарифам и не активируется повторно.
  Владелец может полностью редактировать и удалять промокоды.
- Добавлено дарение Premium собеседнику и поиск по псевдониму.
- Добавлены фото для всех пользователей и Premium-вложения в анонимном чате:
  GIF, видео, голосовые и видеосообщения. Доступно подтверждаемое раскрытие профиля.
- Уведомления о симпатии и новом сообщении уходят в чат с ботом, когда пользователь
  не находится в MiniApp.
- Модератор видит только функции модерации и не видит владельца или других
  модераторов. Назначение модераторов, цены и промокоды доступны только владельцу.
- Предупреждения модерации доставляются пользователю отдельным сообщением бота.
- Реферал засчитывается автоматически после выполнения условий. HMAC-реестр
  защищает от повторной регистрации, поздней подстановки кода и повторного получения
  награды после удаления аккаунта; подозрительные аккаунты не получают автозачисление.

## Автоматическая проверка

| Проверка                         | Результат                             |
| -------------------------------- | ------------------------------------- |
| Secret scan                      | passed                                |
| Prettier                         | passed                                |
| ESLint                           | passed                                |
| TypeScript `strict`              | passed                                |
| Unit/integration/migration tests | 76 passed                             |
| Playwright E2E                   | 123 passed: Android, iPhone и desktop |
| Production build                 | passed                                |
| Dependency audit                 | известных уязвимостей нет             |
| `git diff --check`               | passed                                |

Регрессионные сценарии отдельно проверяют поиск между разными пользователями,
промокоды и их резервы, подарки Premium, уведомления о начислении, суточный буст,
публикацию медиа без очереди, быстрые действия модерации, предупреждения,
изолированный RBAC, сохранение анкеты, предпросмотр, реферальную защиту,
принудительное снятие Premium-возможностей после истечения entitlement, бесплатные
входящие симпатии, прямой чат, музыкальные метаданные, порядок карусели,
ограничения аватара, ротацию CSRF, единый поиск, модерацию публичных профилей,
анкет и постов и восстановление каждого маршрута меню.

## D1 и production smoke

Перед миграциями проверено отсутствие конфликтующих резервов промокодов. После
миграций:

- `PRAGMA foreign_key_check` не вернул нарушений;
- ожидающих миграций нет;
- дубли активных резервов промокодов отсутствуют;
- снимки скидок согласованы с существующими активациями;
- ожидающие медиа переведены в автоматическую публикацию, отклонённые медиа не
  восстанавливаются;
- `boost_cooldown_days = 1`;
- таблица `referral_identity_claims` создана;
- поля музыки и технические метаданные аватаров добавлены без изменения существующих
  четырёх production-медиафайлов;
- источник связи `mutual/direct` добавлен без потери совпадений;
- количество production-строк до и после `0019`–`0021` совпало:
  1 профиль, 4 медиафайла, 0 совпадений;
- сильный случайный `REFERRAL_IDENTITY_SECRET` установлен отдельно для preview и
  production, его значение не хранится в репозитории.

Публичная проверка после развёртывания:

| Endpoint                      | Результат                  |
| ----------------------------- | -------------------------- |
| App `/health/live`            | 200, `ok`                  |
| App `/health/ready`           | 200, D1 `true`             |
| App `/version`                | 200, production            |
| Data API `/health/live`       | 200, `ok`                  |
| Data API `/health/ready`      | 200, `ready`               |
| MiniApp JS/CSS                | 200, `index-B9SfJfFw.js`   |
| Public profile API без сессии | 401, маршрут опубликован   |
| Questionnaires API без сессии | 401, маршрут опубликован   |
| Posts API без сессии          | 401, маршрут опубликован   |
| Global search API без сессии  | 401, маршрут опубликован   |
| Admin profiles без сессии     | 401, маршрут опубликован   |
| Menu auth с ложной подписью   | 401, `INVALID_MENU_LAUNCH` |
| User API без сессии           | 401                        |
| Data API без подписи          | 401                        |

## Честно оставшиеся внешние проверки

1. Реальную оплату или возврат Telegram Stars нельзя автоматизировать без фактической
   транзакции. Серверная обработка invoice, pre-checkout, successful payment,
   идемпотентности, подарка и промокода покрыта тестами.
2. Полный ручной сценарий с двумя реальными Telegram-пользователями — взаимная
   симпатия, relay и медиа — остаётся клиентской приёмкой; автоматические серверные и
   браузерные сценарии проходят.
3. Для настоящих аудио- и видеозвонков нужны внешние Cloudflare Calls/TURN
   credentials. Без них интерфейс не выдаёт ложный работающий звонок; чат и все
   остальные вложения работают независимо.
4. Проверка подписки на канал/супергруппу требует, чтобы RoleMate был администратором.
   Членство в стороннем боте Telegram Bot API не раскрывает, поэтому для ботов
   используется подписанный одноразовый callback.
5. Настоящие Telegram Premium custom emoji требуют выбранных `custom_emoji_id`.
   До их предоставления используется безопасный Unicode fallback.
