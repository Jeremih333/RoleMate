# Финальный отчёт проверки RoleMate

Дата: 29 июля 2026 года.

## Production

- App Worker: `https://rolemate-app.carreljeremih.workers.dev`
- App Worker version: `2f1a2642-51db-419e-88b2-5497d0e112da`
- Data API: `https://rolemate-data-api.carreljeremih.workers.dev`
- Data API version: `4d41631d-a77f-4116-811a-a4f99e2e3171`
- Preview Data API version: `e9c0cdcb-7eb1-4c3f-aa95-bf2c2e97def1`
- D1 migrations: `0001`–`0011` применены к preview и production.
- Time Travel bookmarks перед 0011:
  - production: `0000000a-00000dfc-000050b6-dfaa2ef08d5762e67218d3bc2f5a6caf`;
  - preview: `00000004-00000002-000050b6-7fbf6b6137cb2bff3efd012ad1d87bc6`.
- Production остаётся единым Cloudflare App Worker с D1 и не требует Northflank или банковской карты.

## Реализовано в этом выпуске

- публикации через бота вне MiniApp: создание, лента, удаление, жалобы, блокировка и переход к общению;
- оценки собеседников, публичные лайки/дизлайки и влияние рейтинга на порядок анкет и публикаций;
- автоматическая публикация корректных анкет и модераторская блокировка по жалобе, Telegram ID или списку;
- Premium-ограничения для видео, GIF, аудио, ссылок и скрытия возраста/пола;
- проверка Premium-ссылок: разрешены проверяемые Telegram-профили и каналы, боты и группы отклоняются;
- единые подсказки о покупке Premium в боте и MiniApp;
- промокоды владельца: скидка в Stars, резерв рублёвой скидки для будущего внешнего метода, тарифы, Premium-дни, срок и лимит активаций;
- обязательная подписка в ленте после трёх публикаций, повтор через сутки, проверка каналов/супергрупп и кооперативная проверка сторонних ботов;
- новостной канал `https://t.me/rolemate`;
- назначение и отзыв модераторов владельцем по Telegram ID;
- управление модераторами непосредственно в отдельном разделе админ-панели владельца;
- раздел «Пользователи» показывает только Telegram-аккаунты и больше не подставляет имя анкеты;
- отдельный RBAC модератора: пользователи, анкеты, медиа и жалобы доступны; платежи, Premium-выдача, цены, промокоды, рассылки, реклама, настройки, система и аудит закрыты сервером и интерфейсом;
- исправлен запуск из `/menu`: подписанный `tgWebAppData` восстанавливается из Telegram URL, если WebApp SDK загрузился поздно.
- пользователь может отключить и снова включить свою одобренную анкету; скрытие модерацией самостоятельно обойти нельзя;
- языки вводятся тегами: доступны подсказки, пользовательские значения, Enter и разделение запятыми;
- добавлены 37 понятных вариантов часовых поясов, включая «по Москве» и «по Екатеринбургу»;
- добавлены пользовательские теги анкеты и поиск по псевдониму, заголовку, описанию, фандомам, жанрам, сеттингам, сюжетам и тегам;
- описание анкеты поддерживает безопасный Markdown-предпросмотр и отображение; ссылки перестают быть активными сразу после истечения Premium;
- без Premium разрешено одно фото анкеты, с Premium — до восьми фото, GIF, видео, музыкальных, голосовых и поддерживаемых медиафайлов;
- Premium-возможности проверяются по действующему entitlement при каждом запросе: после истечения скрываются дополнительные медиа, демографические настройки и расширенные фильтры;
- блокировка написавшего пользователя доступна у каждого диалога в MiniApp и в меню анонимного чата бота;
- буквенные заглушки заменены аватаром RoleMate, а карточки новостей и пиар-чата получили отдельные изображения;
- исправлено горизонтальное переполнение текста и элементов на узких экранах;
- Cloudflare rate limiter теперь ведёт отдельный счётчик для каждого маршрута: обычная работа MiniApp больше не блокирует `/api/auth/telegram`, отдельный лимит входа сохранён;
- модераторы не видят staff-аккаунты, их анкеты, публикации, медиа и жалобы и не могут воздействовать на владельца или других модераторов; владелец сохраняет полный доступ.

## Автоматическая проверка

| Проверка                         | Результат                                       |
| -------------------------------- | ----------------------------------------------- |
| Secret scan                      | passed                                          |
| Prettier                         | passed                                          |
| ESLint                           | passed                                          |
| TypeScript strict typecheck      | passed                                          |
| Unit/integration/migration tests | 52 passed                                       |
| Playwright E2E                   | 51/51 passed на small Android, iPhone и desktop |
| Production build                 | passed                                          |
| Dependency audit                 | известных уязвимостей нет                       |
| Preview migration + Worker smoke | passed, `ready`                                 |

Regression-тесты отдельно проверяют отключение анкеты, поиск по тегам, немедленное
снятие Premium-медиа после истечения entitlement, сохранность единственного
бесплатного фото, изоляцию rate-limit маршрутов, блокировку написавшего
пользователя, сокрытие staff-аккаунтов от модераторов и позднюю загрузку Telegram
SDK из `/menu`.

## Production smoke

| Проверка                      | Результат                        |
| ----------------------------- | -------------------------------- |
| App `/health/live`            | 200, `ok`                        |
| App `/health/startup`         | 200, `started`                   |
| App `/health/ready`           | 200, D1 `true`                   |
| App `/version`                | 200                              |
| Data API `/health/ready`      | 200, `ready`                     |
| Production MiniApp asset      | `assets/index-LnmcNHMF.js`, 200  |
| News avatar asset             | 200, `image/webp`, 16 780 bytes  |
| Auth после 40 API-запросов    | 401 для неверной подписи, не 429 |
| Admin API без сессии          | 401                              |
| D1 migration list             | нет ожидающих миграций           |
| D1 `PRAGMA foreign_key_check` | нарушений нет                    |

## Честно оставшиеся внешние проверки

1. После исправлений `/menu` и `RATE_LIMITED` нужен один повторный ручной вход владельца в реальном Telegram-клиенте. Автоматический сценарий и production invalid-signature smoke проходят.
2. Реальную Stars-покупку/возврат и двусторонний relay нельзя безопасно завершить без действий двух реальных пользователей и фактического платежа.
3. Telegram Bot API проверяет членство в канале/супергруппе через `getChatMember`, если RoleMate — администратор. Членство в стороннем боте Bot API не раскрывает, поэтому для ботов реализован подписанный callback с одноразово показываемым секретом.
4. Рублёвая скидка хранится для будущего внешнего метода, но не применяется к цифровому Premium внутри Telegram: там разрешены только Stars (`XTR`).
5. Для настоящих Telegram Premium custom emoji нужны реальные `custom_emoji_id`; пока используется безопасный Unicode fallback.
6. `.github/workflows/ci.yml` не изменялся, так как для этого требуется отдельная команда «Исправляй CI».
